/**
 * Sistema di Test (SEMPLIFICATO)
 * 
 * Esporta:
 * - Schema DB (sessions con answers JSONB)
 * - Banca domande (chiuse + aperte per sezione)
 * - Assembler (risposte → ProfileData → embedding)
 */

export * from "./schema";
export * from "./questions";
export * from "./assembler";

